rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Server-side API routes use Admin SDK (bypasses rules entirely).
      // Client pages only read — writes from the browser are blocked.
      allow read: if true;
      allow write: if false;
    }
  }
}
